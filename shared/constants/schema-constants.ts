import { SCHEMA_PREFIX } from "./general-constants";

export enum SchemaConstants {
    User = `${SCHEMA_PREFIX}:user`,
    Schema = `${SCHEMA_PREFIX}:schema`,
    Login = `${SCHEMA_PREFIX}:login`
}   